import { ScrollSuaveDirective } from './scroll-suave.directive';

describe('ScrollSuaveDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollSuaveDirective();
    expect(directive).toBeTruthy();
  });
});
